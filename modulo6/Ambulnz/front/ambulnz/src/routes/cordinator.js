export const goToHome = (history) => {
    history.push('/')
}
export const gotoCart = (history) => {
    history.push('/carrinho')
}
export const goToOrders = (history) => {
    history.push('/pedidos')
}